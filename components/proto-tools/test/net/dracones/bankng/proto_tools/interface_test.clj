(ns net.dracones.bankng.proto-tools.interface-test
  (:require [clojure.test :as test :refer :all]
            [net.dracones.bankng.proto-tools.interface :as proto-tools]))

(deftest dummy-test
  (is (= 1 1)))
