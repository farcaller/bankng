(ns net.dracones.bankng.frontend-service.interface-test
  (:require [clojure.test :as test :refer :all]
            [net.dracones.bankng.frontend-service.interface :as frontend-service]))

(deftest dummy-test
  (is (= 1 1)))
