(ns net.dracones.bankng.accounts.interface-test
  (:require [clojure.test :as test :refer :all]
            [net.dracones.bankng.accounts.interface :as accounts]))

(deftest dummy-test
  (is (= 1 1)))
