(ns net.dracones.bankng.pb-frontend.interface-test
  (:require [clojure.test :as test :refer :all]
            [net.dracones.bankng.pb-frontend.interface :as pb-frontend]))

(deftest dummy-test
  (is (= 1 1)))
