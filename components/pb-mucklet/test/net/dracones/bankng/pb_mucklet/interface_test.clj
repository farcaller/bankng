(ns net.dracones.bankng.pb-mucklet.interface-test
  (:require [clojure.test :as test :refer :all]
            [net.dracones.bankng.pb-mucklet.interface :as pb-mucklet]))

(deftest dummy-test
  (is (= 1 1)))
