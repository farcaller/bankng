(ns net.dracones.bankng.codesender.interface-test
  (:require [clojure.test :as test :refer :all]
            [net.dracones.bankng.codesender.interface :as codesender]))

(deftest dummy-test
  (is (= 1 1)))
